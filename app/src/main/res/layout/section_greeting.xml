<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="24dp">

    <!-- Fila superior: saludo + chip Eco-Puntos -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Texto de saludo (ocupa el espacio restante) -->
        <TextView
            android:id="@+id/tv_greeting"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Buenas tardes,\nEstudiante!"
            android:textColor="@color/text_primary"
            android:textSize="28sp"
            android:textStyle="bold"
            android:lineSpacingMultiplier="1.1"
            tools:text="Buenas tardes,\nEstudiante!" />

        <!-- Chip de Eco-Puntos (ODS 12 — gamificación sostenible) -->
        <TextView
            android:id="@+id/tv_eco_points_total"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="🌱 0 Eco-Puntos"
            android:textColor="@color/eco_green_badge_text"
            android:textSize="11sp"
            android:textStyle="bold"
            android:gravity="center"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:background="@drawable/bg_badge_green"
            tools:text="🌱 12 Eco-Puntos" />

    </LinearLayout>

    <!-- Subtítulo motivacional -->
    <TextView
        android:id="@+id/tv_greeting_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="20dp"
        android:text="Hagamos de hoy un día productivo y sostenible"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        tools:text="Hagamos de hoy un día productivo y sostenible" />

</LinearLayout>
